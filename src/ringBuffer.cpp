///The ring buffer will prove useful in the C/S systems, eventually.
///It's like a FIFO (First In First Out) queue, but it doesn't need to allocate any memory, making it quite a bit faster.

#include "ringBuffer.h"
